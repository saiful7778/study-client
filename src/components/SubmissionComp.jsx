import PropTypes from "prop-types";

const SubmissionComp = ({ assignmentId }) => {
  return <div>SubmissionComp</div>;
};

SubmissionComp.propTypes = {
  assignmentId: PropTypes.string,
};

export default SubmissionComp;
